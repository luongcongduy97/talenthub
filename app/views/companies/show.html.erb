<div class="max-w-md mx-auto bg-white shadow rounded p-6">
  <h1 class="text-2xl font-bold mb-3"><%= @company.name %></h1>
  <p class="mb-2"><strong>Industry:</strong> <%= @company.industry %></p>
  <div>
    <%= link_to 'Edit', edit_company_path(@company), class: 'px-4 py-2 bg-indigo-600 text-white rounded' %>
    <%= link_to 'Back', companies_path, class: 'px-4 py-2 bg-gray-600 text-white rounded' %>
  </div>
</div>
